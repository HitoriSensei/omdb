declare interface PrivateConfig {
  // Extend this interface with custom values you want to add to config/private.ts
}
declare interface PublicConfig {
  // Extend this interface with custom values you want to add to config/public.ts
}
